# tales-of-malmo

## Project setup
```
npm install
```

## Build and run

To build, with a watch, run:

```
npm run serve

# or to just build and not watch
npm run build
```

To start a server run:

```
node ./server
```
